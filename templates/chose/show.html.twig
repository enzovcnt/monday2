{% extends 'base.html.twig' %}

{% block title %}Chose{% endblock %}

{% block body %}
    <h1>Chose</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chose.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ chose.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chose_index') }}">back to list</a>

    <a href="{{ path('app_chose_edit', {'id': chose.id}) }}">edit</a>

    {{ include('chose/_delete_form.html.twig') }}
{% endblock %}
